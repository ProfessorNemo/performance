en:
  activerecord:
    errors:
      models:
        event:
          attributes:
            starts_at:
              overlap: "shouldn't overlaps with other events periods"
            ends_at:
              overlap: "shouldn't overlaps with other events periods"
